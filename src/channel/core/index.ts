// ---------------------------------------------------------------------------
// Core abstractions — shared across all deployment targets (Node / Worker)
// ---------------------------------------------------------------------------

export type {
  BitableAdapter,
  FeishuAdapter,
  ExecutorPoolInterface,
  ExecutorInfo,
  Scheduler,
  Logger,
  ChannelEnv,
  TicketRecord,
  TurnRecord,
  RoundRecord,
  RosterRecord,
} from './types.js';
